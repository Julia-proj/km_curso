# Навигация в тесте: Назад и Заново

## ✅ Что добавлено

### 1. Кнопка "Назад" в тесте (уже была, улучшена)
**Где:** `/quiz` - QuizContainer компонент

**Функционал:**
- Кнопка появляется со 2-го вопроса (когда `currentStep > 0`)
- При клике возвращает на предыдущий вопрос
- Сохраняет все предыдущие ответы
- Можно изменить ответ и продолжить дальше

**Стиль:**
- Обновлён на editorial-стиль (border, paper background)
- Соответствует дизайн-системе проекта
- CSS-переменные вместо хардкода цветов

### 2. Кнопка "Пройти тест заново" (новая)
**Где:** `/result` - страница результатов теста

**Функционал:**
- Сбрасывает все ответы через `reset()`
- Перенаправляет на начало теста `/quiz`
- Можно пройти тест с нуля, если результат не устраивает

**Расположение:**
- Под главной CTA кнопкой "Выбрать тариф"
- Над ссылкой "Бесплатный урок"
- Вторичный стиль (не отвлекает от главного действия)

## 🎨 Визуальный стиль

Обе кнопки используют:
- `border: 1px solid var(--color-line)` - тонкая рамка
- `background: var(--color-paper)` - фон как у карточек
- `color: var(--color-ink-soft)` - приглушённый текст
- `border-radius: 2px` - минимальное скругление (правило проекта)

Это соответствует editorial-стилю HAIRLAB (не SaaS).

## 🧪 Как протестировать

### Кнопка "Назад":
```bash
npm run dev
# Открой http://localhost:3000/quiz
```

1. Ответь на 1-й вопрос
2. На 2-м вопросе появится кнопка "← Назад"
3. Нажми - вернёшься на 1-й вопрос
4. Твой предыдущий ответ сохранён (выделен)
5. Можешь изменить и продолжить

### Кнопка "Пройти тест заново":
```bash
npm run dev
# Открой http://localhost:3000/quiz
```

1. Пройди весь тест (10 вопросов)
2. Попадёшь на `/result`
3. Увидишь:
   - Результаты теста
   - CTA "Выбрать тариф"
   - **"← Пройти тест заново"** (новая кнопка)
   - "Бесплатный урок"
4. Нажми "Пройти тест заново"
5. Вернёшься на `/quiz` с чистого листа

## 📝 Изменённые файлы

- `src/components/quiz/QuizContainer.tsx` - улучшен стиль кнопки "Назад"
- `src/app/result/page.tsx` - добавлена кнопка "Пройти тест заново"

## 🔄 Логика работы

### Store (quiz-store.ts):
Уже был готов:
- `prevStep()` - уменьшает currentStep
- `reset()` - сбрасывает currentStep и answers

### Навигация:
- "Назад" → `prevStep()` - шаг назад без потери данных
- "Заново" → `reset()` + `router.push('/quiz')` - полный сброс

### Сохранение ответов:
- Ответы сохраняются в localStorage через zustand persist
- При возврате назад ответ остаётся выбранным
- Можно изменить и продолжить
- При "Заново" всё удаляется

## ✨ UX улучшения

**До:**
- ❌ Нельзя вернуться назад, если ошибся
- ❌ Нельзя пройти тест заново с результатов

**После:**
- ✅ Можно вернуться на любой вопрос назад
- ✅ Можно изменить ответ и продолжить
- ✅ Можно пройти тест заново с результатов
- ✅ Ответы сохраняются при возврате
